import React from 'react'
import Login from './login'

export const metadata = {
    title: "Login : Work Miner"
}

const loginPage = () => {
  return (
    <div>
        <Login />
    </div>
  )
}

export default loginPage